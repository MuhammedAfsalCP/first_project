import {createSlice} from '@reduxjs/toolkit'

const initialState={
    products:[],
    showModal:false
}

const State=createSlice({
    name :"state",
    initialState,
    reducers:{
        
    }
})